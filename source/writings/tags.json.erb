<%
  tag_array =  []
  blog('writings').tags.each do |tag, articles|
    if articles.size > 1
      tag_array << {text: tag, weight: articles.size, link: "#tag-#{tag}"}
    end
  end 
%>
<%= tag_array.to_json %>